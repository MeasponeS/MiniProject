import { VantComponent } from '../vant-weapp/common/component';
VantComponent({
    props: {
        // 这里定义了innerText属性，属性值可以在组件使用时指定
        title:{
            type:String,
            value:"",
        },
        options:{
            type:Array,
            value:[],
        }
    },
    data: {

    },

    methods: {

    }
});
